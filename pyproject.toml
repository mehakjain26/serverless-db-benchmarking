[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gtfs-bench"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["server*"]

[tool.setuptools]
py-modules = ["req_gen", "globals", "locust_base"]
